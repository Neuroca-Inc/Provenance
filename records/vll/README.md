# VLL Provenance Records

These files are selected as methodology-in-action evidence.

## Rung-1 mechanism decision v0.3

The document explicitly refuses to treat a guessed continuum-to-graph substitution as established CF04 structure. It identifies the unstated bridge, withdraws the earlier implementation requirement, and splits the next work into a derivation/admission checkpoint followed by capability replay.

Classification: `CORRECTION_RECORD`

## Rung-0 run package

Selected machine-readable run artifacts preserve:

- run identity;
- plan SHA-256;
- package code hashes;
- stage commands/status;
- run manifest;
- provenance plan;
- decisive experiment summaries.

Large state snapshots and compiled/cache artifacts were intentionally not copied into this repository.
