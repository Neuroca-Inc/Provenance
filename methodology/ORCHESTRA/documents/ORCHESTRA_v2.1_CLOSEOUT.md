# Package Closeout Map

`CLOSEOUT.json` is machine authority. Complete this before finalization.

## Decisive package contents

| Artifact | Role | Why it matters |
|---|---|---|
| `REPLACE_ME` | decisive | REPLACE_ME |

## Important claims

| Claim | Why successors need it |
|---|---|
| C001 | REPLACE_ME |

## Ancestor disposition

Every ancestor in `ANCESTRY.json` must appear exactly once below.

| Ancestor | Relevance after this package | Why | Successor instruction |
|---|---|---|---|
| A001 | REPLACE_ME | REPLACE_ME | REPLACE_ME |

Allowed relevance values:

- `CONTROLLING`: still semantically authoritative for successors.
- `STILL_RELEVANT`: still needed, but this package adds or narrows it.
- `SUPERSEDED_BY_THIS_PACKAGE`: this package replaces it for the stated scope; preserve it for provenance.
- `HISTORICAL_ONLY`: useful for chronology/discovery history, not current authority.
- `NO_LONGER_RELEVANT`: no longer needed by successors; state why.

## Successor start-here set

List the smallest exact set a successor must read first. This is not a summary of the package; it is the handoff frontier.

- `REPLACE_ME`
